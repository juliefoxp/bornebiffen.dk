---
const { htmlheading, size = "medium", font = "sans", stylingclasses = "" } = Astro.props;

if (!htmlheading) {
  throw new Error("Heading-komponenten kræver en 'htmlheading' prop, fx 'h1', 'h2' eller 'h3'.");
}

// Tailwind classes for størrelser med responsivitet
const sizeClasses = {
  "large": "text-4xl sm:text-5xl md:text-6xl lg:text-8xl",
  "medium": "text-2xl sm:text-3xl md:text-4xl lg:text-4xl",
  "small": "text-xl sm:text-2xl md:text-2xl lg:text-2xl",
};

// Tailwind classes for skrifttyper
const fontClasses = {
  "display": "font-display", // Fraunces
  "sans": "font-sans",       // Nobile
  "body": "font-body",       // News Cycle
};
---

{htmlheading === "h1" && (
  <h1 class:list={[sizeClasses[size], fontClasses[font], stylingclasses]}>
    <slot />
  </h1>
)}

{htmlheading === "h2" && (
  <h2 class:list={[sizeClasses[size], fontClasses[font], stylingclasses]}>
    <slot />
  </h2>
)}

{htmlheading === "h3" && (
  <h3 class:list={[sizeClasses[size], fontClasses[font], stylingclasses]}>
    <slot />
  </h3>
)}
